# ERP AI SQL Assistant — Python dependencies
# Install with: pip install -r requirement.txt

# LLM
groq>=0.9.0

# Database (ERP)
sqlalchemy>=2.0.0
pyodbc>=5.0.0

# Embeddings & vector search
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0             # use faiss-gpu if you have CUDA

# Env / config
python-dotenv>=1.0.0

# Utilities
numpy>=1.26.0

# ── Auth (new) ───────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-jose[cryptography]>=3.3.0   # JWT
bcrypt>=4.0.0                      # password hashing (no passlib needed)
python-multipart>=0.0.9            # required for OAuth2PasswordRequestForm