# ERP AI SQL Assistant — Python dependencies
# Install with: pip install -r requirements.txt

# LLM
groq>=0.9.0

# Database
sqlalchemy>=2.0.0
pyodbc>=5.0.0

# Embeddings & vector search
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0             # use faiss-gpu if you have CUDA

# Env / config
python-dotenv>=1.0.0

# API layer (already required by api/app.py; pinned here for clarity)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0              # write_models.py uses Pydantic v2 (TypeAdapter, discriminated unions)

# Utilities
numpy>=1.26.0